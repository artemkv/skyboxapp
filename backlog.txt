General

- --Now we retrieve metadata for every folder--

Download

- --After download is done, open the file--
- --While downloading, show some kind of progress indicator--
- Store etags of downloaded files in private folder
- If already downloaded and matches --the size-- and etag, skip download, just open
- Show errors

UI

- Add header

Security

- Add settings screen
- Allow saving settings as JSON (with validation), 1 JSON per device
- Allow selecting active device
- Use those settings to connect to S3
- Store settings encrypted
- Derive encryption key from pin
- Ask for pin on startup
